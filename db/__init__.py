"""
Database bootstrap package (outside `backend/app`).

Application code stays in `app/`; this tree is the place to evolve toward
external DB provisioning, migration runners, or swapping SQLite for another store.
"""
